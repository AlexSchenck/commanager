import { Component } from "@angular/core";

@Component({
    selector: "ns-cards",
    moduleId: module.id,
    templateUrl: "./cards.component.html"
})
export class CardsComponent {
    constructor(
    ) { }
}
